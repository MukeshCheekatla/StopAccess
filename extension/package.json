{
  "name": "focusgate-extension",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "check": "node ../scripts/check-extension.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.2"
  }
}
